<!DOCTYPE html>
<html>
<head>
  <title><%= product.name %> - Eco Saver</title>
  <style>
    body { font-family: Arial, sans-serif; background: #e4ffe6; margin: 0; }
    .container {
      max-width: 680px;
      margin: 30px auto 0 auto;
      background: #fff;
      border-radius: 15px;
      box-shadow: 0 2px 10px #ccc;
      padding: 32px 28px 28px 28px;
    }
    .eco-header {
      font-size: 32px;
      font-weight: bold;
      color: #208068;
      text-align: center;
      position: relative;
    }
    .eco-house-link {
      position: absolute;
      top: 14px;
      right: 32px;
      text-decoration: none;
    }
    .eco-house-icon {
      display: inline-block;
      width: 38px;
      height: 38px;
      background: #47e299;
      border-radius: 10px;
      text-align: center;
      line-height: 38px;
      color: #fff;
      font-size: 30px;
      font-weight: bold;
      box-shadow: 0 0 4px #20806860;
    }
    .product-img {
      width: 100%;
      height: 180px;
      background: #bbecfc;
      margin-bottom: 16px;
      border-radius: 11px;
      object-fit: cover;
      display: block;
    }
    .product-name {
      font-size: 23px;
      font-weight: 600;
      margin-bottom: 10px;
      color: #208068;
    }
    .product-desc {
      font-size: 15px;
      margin-bottom: 17px;
      color: #333;
    }
    .seller-card {
      background: #c0f5d9;
      padding: 15px 16px;
      border-radius: 10px;
      display: flex;
      align-items: center;
      gap: 18px;
      margin-top: 20px;
      box-shadow: 0 1px 3px #2080681a;
    }
    .seller-pic {
      width: 54px;
      height: 54px;
      border-radius: 50%;
      background: #bbecfc;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 20px;
      color: #47e299;
      font-weight: 800;
      overflow: hidden;
    }
    .seller-details {
      display: flex;
      flex-direction: column;
      gap: 3px;
    }
    .seller-ratings {
      color: #f0c543;
      font-size: 20px;
    }
    .seller-label {
      font-weight: 600;
      color: #227c6d;
    }
    .seller-info {
      color: #333;
      font-size: 14px;
    }
  </style>
</head>
<body>
  <div class="eco-header">
    ECO SAVER
    <a href="/home" class="eco-house-link">
      <span class="eco-house-icon">&#8962;</span>
    </a>
  </div>
  <div class="container">
    <img src="<%= product.image || '' %>" class="product-img" alt="Product Image" />
    <div class="product-name"><%= product.name %></div>
    <div class="product-desc"><%= product.description %></div>

    <div class="seller-card">
      <div class="seller-pic">
        <% if(product.seller.pic) { %>
          <img src="<%= product.seller.pic %>" width="54" height="54" style="border-radius:50%;" />
        <% } else { %>
          <span>👤</span>
        <% } %>
      </div>
      <div class="seller-details">
        <div class="seller-label">Seller: <span class="seller-info"><%= product.seller.name %></span></div>
        <div class="seller-label">Email: <span class="seller-info"><%= product.seller.email %></span></div>
        <div class="seller-label">Phone: <span class="seller-info"><%= product.seller.phone %></span></div>
        <div class="seller-label">Ratings: 
          <span class="seller-ratings">
            <% for (let i = 0; i < 5; i++) { %>
              <%= i < product.seller.ratings ? '★' : '☆' %>
            <% } %>
          </span>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
